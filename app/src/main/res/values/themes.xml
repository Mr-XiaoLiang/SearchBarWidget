<resources>
    <!-- Base application theme. -->
    <style name="Theme.SearchBarWidget" parent="Theme.Material3.DayNight">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/theme_first</item>
        <item name="colorPrimaryVariant">@color/theme_first</item>
        <item name="colorOnPrimary">@color/theme_first_on</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/theme_secondary</item>
        <item name="colorSecondaryVariant">@color/theme_secondary</item>
        <item name="colorOnSecondary">@color/theme_secondary_on</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>